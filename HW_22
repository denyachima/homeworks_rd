SELECT *
FROM users
WHERE age > 30;

SELECT COUNT(*)
FROM users
WHERE age > 30;

SELECT age, COUNT(*) AS users
FROM users
GROUP BY age;

SELECT age, COUNT(*) AS users
FROM users
GROUP BY age
ORDER BY users DESC, age ASC;
